import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {},
  video: {
    width: '100%',
    height: '100%',
    aspectRatio: 9 / 16,
  },
});

export default styles;
